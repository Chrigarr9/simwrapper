---
phase: 04.3-scatter-plot-color-by-selector-and-cross-card-sync
plan: 02
type: execute
wave: 2
depends_on: ["04.3-01"]
files_modified:
  - src/plugins/interactive-dashboard/components/cards/ScatterCard.vue
  - src/plugins/interactive-dashboard/components/cards/HistogramCard.vue
autonomous: true

must_haves:
  truths:
    - "ScatterCard colors points by the selected color-by attribute (categorical: per-category traces; numeric: Plotly colorscale)"
    - "ScatterCard shows default single-color rendering when color-by is 'None'"
    - "ScatterCard preserves comparison mode behavior (baseline stays gray, filtered gets color-by colors)"
    - "HistogramCard colors bars by the selected categorical color-by attribute using stacked traces per category"
    - "HistogramCard colors bars by average value for numeric color-by attributes"
    - "HistogramCard preserves comparison mode behavior"
    - "Type detection auto-detects categorical vs numeric from actual data values (not YAML)"
  artifacts:
    - path: "src/plugins/interactive-dashboard/components/cards/ScatterCard.vue"
      provides: "Color-by rendering with categorical multi-trace and numeric colorscale support"
      contains: "colorByAttribute"
    - path: "src/plugins/interactive-dashboard/components/cards/HistogramCard.vue"
      provides: "Color-by rendering with stacked categorical and average-colored numeric"
      contains: "colorByAttribute"
  key_links:
    - from: "ScatterCard.vue"
      to: "StyleManager"
      via: "buildCategoricalColorMap() and getSequentialColor()"
      pattern: "buildCategoricalColorMap|getSequentialColor"
    - from: "HistogramCard.vue"
      to: "StyleManager"
      via: "buildCategoricalColorMap() for stacked bar colors"
      pattern: "buildCategoricalColorMap"
---

<objective>
Add color-by attribute support to ScatterCard and HistogramCard, enabling them to render data with colors driven by the dashboard-level colorByAttribute state.

Purpose: Extends color-by from a MapCard-only feature to scatter and histogram visualizations, enabling consistent cross-card coloring.

Output: Updated ScatterCard.vue and HistogramCard.vue with color-by rendering logic.
</objective>

<execution_context>
@/home/christoph/.claude/get-shit-done/workflows/execute-plan.md
@/home/christoph/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04.3-scatter-plot-color-by-selector-and-cross-card-sync/04.3-CONTEXT.md
@.planning/phases/04.3-scatter-plot-color-by-selector-and-cross-card-sync/04.3-RESEARCH.md
@.planning/phases/04.3-scatter-plot-color-by-selector-and-cross-card-sync/04.3-01-SUMMARY.md
@src/plugins/interactive-dashboard/components/cards/ScatterCard.vue
@src/plugins/interactive-dashboard/components/cards/HistogramCard.vue
@src/plugins/interactive-dashboard/managers/StyleManager.ts (lines 709-788 for color APIs)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add color-by support to ScatterCard</name>
  <files>src/plugins/interactive-dashboard/components/cards/ScatterCard.vue</files>
  <action>
Add `colorByAttribute` prop to the Props interface:
```typescript
colorByAttribute?: string  // Dashboard-level color-by attribute
colorByOptions?: Array<{ attribute: string; label: string; type: string }>
```

Add a type detection utility function (NOT in YAML — auto-detect from data per user requirement):
```typescript
const detectColorByType = (attribute: string): 'categorical' | 'numeric' => {
  if (!props.filteredData?.length || !attribute) return 'categorical'
  const values = props.filteredData
    .map(row => row[attribute])
    .filter(v => v !== null && v !== undefined)
  if (values.length === 0) return 'categorical'

  // Check if all values are numbers
  const allNumeric = values.every(v => typeof v === 'number' && !isNaN(v))
  if (!allNumeric) return 'categorical'

  // If numeric but few unique values (< 15), treat as categorical
  // This handles cases like mode IDs (1=car, 2=bike)
  const uniqueValues = new Set(values)
  if (uniqueValues.size < 15) return 'categorical'

  return 'numeric'
}
```

Modify the chart rendering logic (the `renderChart` or equivalent function) to handle color-by:

**When `colorByAttribute` is set and type is 'categorical':**
- Instead of one trace, create SEPARATE traces per category value
- Get sorted unique values of the color-by attribute from data
- Use `StyleManager.getInstance().buildCategoricalColorMap(sortedValues)` for consistent colors
- Each trace: filter data to that category, set `marker.color` to the category's color, set `name` to the category value
- Show legend (`showlegend: true`) so categories are labeled

**When `colorByAttribute` is set and type is 'numeric':**
- Use single trace with Plotly's built-in colorscale
- Set `marker.color` to array of attribute values
- Set `marker.colorscale` to 'Viridis'
- Set `marker.showscale: true` with `colorbar: { title: attributeLabel }`

**When `colorByAttribute` is '' or undefined (None):**
- Use existing single-trace behavior (no change from current code)

**Comparison mode interaction:**
- When `showComparison` is true:
  - Baseline trace stays gray (rgba(156,163,175,0.3)) regardless of color-by — existing pattern preserved
  - Filtered traces get color-by colors
- The baseline trace should remain a single trace (not split per category) to keep it visually muted

**Plotly event handler preservation:**
- CRITICAL: Do NOT re-register `.on('plotly_click', ...)` or `.on('plotly_hover', ...)` handlers when color-by changes. Follow the existing pattern: register handlers ONCE in initialization, use `Plotly.react()` for updates.
- When clicking a point in multi-trace mode, use `event.points[0].customdata` (not trace index) to identify the data row. Store row IDs in `customdata` for each trace.

**Watch for colorByAttribute changes:**
- Add `props.colorByAttribute` to the existing watch that triggers `renderChart()`
  </action>
  <verify>
- `npm run build` succeeds
- `grep -n "colorByAttribute" src/plugins/interactive-dashboard/components/cards/ScatterCard.vue` shows prop definition and usage
- `grep -n "buildCategoricalColorMap\|getSequentialColor\|colorscale" src/plugins/interactive-dashboard/components/cards/ScatterCard.vue` shows color logic
- `grep -n "detectColorByType" src/plugins/interactive-dashboard/components/cards/ScatterCard.vue` shows type detection
  </verify>
  <done>
- ScatterCard accepts colorByAttribute prop
- Categorical color-by creates per-category traces with StyleManager colors
- Numeric color-by uses Plotly colorscale with Viridis
- "None" (empty string) falls back to default single-color rendering
- Comparison mode baseline stays gray, filtered data gets color-by colors
- No Plotly event handler memory leaks
  </done>
</task>

<task type="auto">
  <name>Task 2: Add color-by support to HistogramCard</name>
  <files>src/plugins/interactive-dashboard/components/cards/HistogramCard.vue</files>
  <action>
Add `colorByAttribute` prop to the Props interface:
```typescript
colorByAttribute?: string  // Dashboard-level color-by attribute
colorByOptions?: Array<{ attribute: string; label: string; type: string }>
```

Add the same type detection utility (or import a shared one if Task 1 creates a utility file — use discretion on whether to extract to a shared file or duplicate the small function).

Modify the chart rendering logic:

**When `colorByAttribute` is set and type is 'categorical':**
- Create STACKED bar traces: one trace per category value
- For each category, compute histogram bins using the same bin edges as the main histogram
- Use `StyleManager.getInstance().buildCategoricalColorMap(sortedValues)` for colors
- Set Plotly layout `barmode: 'stack'` to show category composition per bin
- Each trace gets `name: categoryValue` for legend
- Show legend when stacked

**When `colorByAttribute` is set and type is 'numeric':**
- Keep single trace (histogram bins the column on x-axis as usual)
- Color each bar by the AVERAGE value of the color-by attribute within that bin
- Use Plotly `marker.color` array with computed averages
- Use Plotly `marker.colorscale: 'Viridis'` and `marker.showscale: true`

**When `colorByAttribute` is '' or undefined (None):**
- Use existing single-trace behavior (no change)

**Comparison mode interaction:**
- When `showComparison` is true AND color-by is active:
  - Baseline bars stay gray (existing pattern)
  - Filtered data gets color-by stacked traces
  - Use `barmode: 'overlay'` for baseline behind, but within the filtered group use stacking
  - Implementation approach: render baseline as single gray trace first, then add stacked colored traces for filtered data on top

**Click-to-filter behavior:**
- When clicking a colored bar in stacked mode, the filter should apply to the histogram column's bin (existing behavior), NOT the color-by category. The color-by is purely visual — filtering is the histogram's job.
- Preserve existing `selectedBins` logic for click handling

**Watch for colorByAttribute changes:**
- Add `props.colorByAttribute` to the existing watch that triggers chart re-render
  </action>
  <verify>
- `npm run build` succeeds
- `grep -n "colorByAttribute" src/plugins/interactive-dashboard/components/cards/HistogramCard.vue` shows prop definition and usage
- `grep -n "barmode.*stack\|buildCategoricalColorMap" src/plugins/interactive-dashboard/components/cards/HistogramCard.vue` shows stacking logic
  </verify>
  <done>
- HistogramCard accepts colorByAttribute prop
- Categorical color-by creates stacked bar traces showing category distribution per bin
- Numeric color-by colors single bars by average attribute value per bin
- "None" falls back to default single-color histogram
- Click-to-filter still filters by histogram bin (not color-by category)
- Comparison mode baseline stays gray
- Build passes
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes with both card changes
2. ScatterCard renders multi-trace scatter for categorical color-by
3. ScatterCard renders colorscale scatter for numeric color-by
4. HistogramCard renders stacked bars for categorical color-by
5. HistogramCard renders average-colored bars for numeric color-by
6. Both cards fall back to default rendering when colorByAttribute is empty
7. Both cards preserve comparison mode (baseline gray, filtered colored)
</verification>

<success_criteria>
- ScatterCard and HistogramCard both respond to colorByAttribute prop changes
- Color-by type (categorical vs numeric) is auto-detected from data values
- StyleManager is used for all color assignment (no hardcoded color arrays)
- Comparison mode interaction preserved per user requirement
</success_criteria>

<output>
After completion, create `.planning/phases/04.3-scatter-plot-color-by-selector-and-cross-card-sync/04.3-02-SUMMARY.md`
</output>
