---
phase: 04.3-scatter-plot-color-by-selector-and-cross-card-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/plugins/interactive-dashboard/components/controls/ColorBySelector.vue
  - src/plugins/interactive-dashboard/InteractiveDashboard.vue
autonomous: true

must_haves:
  truths:
    - "Central color-by dropdown appears in the dashboard table header area"
    - "Dropdown shows configured attributes from top-level colorBy YAML section plus a 'None' option"
    - "First configured attribute is active by default on load (not 'None')"
    - "Selecting a different attribute updates the shared colorByAttribute state"
    - "Selecting 'None' disables color-by coloring"
    - "colorByAttribute prop is passed to ALL card types (scatter, map, histogram, pie) via template"
  artifacts:
    - path: "src/plugins/interactive-dashboard/components/controls/ColorBySelector.vue"
      provides: "Reusable dropdown component extracted from MapCard's color-by selector"
      min_lines: 30
    - path: "src/plugins/interactive-dashboard/InteractiveDashboard.vue"
      provides: "Dashboard-level colorBy YAML parsing, state management, selector placement in header, prop passing to all cards"
      contains: "ColorBySelector"
  key_links:
    - from: "InteractiveDashboard.vue"
      to: "ColorBySelector.vue"
      via: "component import and template usage"
      pattern: "ColorBySelector|color-by-selector"
    - from: "InteractiveDashboard.vue (colorBy YAML)"
      to: "colorByAttribute data property"
      via: "mounted() initialization from yaml.colorBy"
      pattern: "yaml\\.colorBy"
    - from: "InteractiveDashboard.vue (colorByAttribute)"
      to: "card components"
      via: ":color-by-attribute prop on component element"
      pattern: "color-by-attribute"
---

<objective>
Extract MapCard's color-by dropdown into a shared ColorBySelector component, place it in the dashboard table header area, and wire up dashboard-level colorBy YAML configuration with shared state management.

Purpose: Establishes the central control and state infrastructure that all card types will consume. This is the foundation for cross-card color synchronization.

Output: ColorBySelector.vue component + InteractiveDashboard.vue modifications for YAML parsing, state, selector placement, and prop passing.
</objective>

<execution_context>
@/home/christoph/.claude/get-shit-done/workflows/execute-plan.md
@/home/christoph/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04.3-scatter-plot-color-by-selector-and-cross-card-sync/04.3-CONTEXT.md
@.planning/phases/04.3-scatter-plot-color-by-selector-and-cross-card-sync/04.3-RESEARCH.md
@src/plugins/interactive-dashboard/InteractiveDashboard.vue
@src/plugins/interactive-dashboard/components/cards/MapCard.vue (lines 1-20 for dropdown to extract, lines 249-270 for event handling)
@src/plugins/interactive-dashboard/components/controls/ComparisonToggle.vue (sibling control component for pattern reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ColorBySelector component and update InteractiveDashboard YAML parsing and state</name>
  <files>
    src/plugins/interactive-dashboard/components/controls/ColorBySelector.vue
    src/plugins/interactive-dashboard/InteractiveDashboard.vue
  </files>
  <action>
**ColorBySelector.vue** — Create in `components/controls/` (alongside ComparisonToggle.vue).

Extract from MapCard.vue lines 13-20 (the color-by dropdown). Create a `<script setup lang="ts">` component with:

Props interface:
```typescript
interface Props {
  modelValue: string  // current colorByAttribute ('' = None)
  options: Array<{ attribute: string; label: string }>
}
```

Template (Pug): A `.control-item` wrapper with:
- `label.control-label` showing "Color by"
- `select.control-select` bound to modelValue, emitting `update:modelValue` on change
- First option: "None" with value `''` (empty string) — this is FIRST in list per research recommendation
- Then v-for over options array rendering each `{ attribute, label }`

Emit: `'update:modelValue'` with the selected attribute string (or '' for None).

Style (scoped): Reuse the same `.control-item`, `.control-label`, `.control-select` styles from MapCard.vue (lines ~1700-1740 in MapCard). The component should look identical to MapCard's existing dropdown.

**InteractiveDashboard.vue** — Multiple changes:

1. **Import ColorBySelector** alongside other control components (ComparisonToggle, ExportAllButton).

2. **YAML colorBy parsing**: In the `mounted()` method, after `initializeCoordinationLayer()`, add parsing for top-level `yaml.colorBy` section. The YAML structure is:
```yaml
colorBy:
  attributes:
    - attribute: main_mode
      label: Transport Mode
    - attribute: distance
      label: Distance
```
Initialize `colorByAttribute` from `yaml.colorBy.attributes[0].attribute` (first configured = default).
ALSO keep backward compatibility: if `yaml.colorBy` doesn't exist, fall back to existing `yaml.map?.colorBy` parsing (lines 848-852).

3. **Update `colorByOptions` computed property** (line 394): Read from `yaml.colorBy?.attributes` first, fall back to `yaml.map?.colorBy?.attributes`. This promotes colorBy from map-level to dashboard-level while keeping backward compat.

4. **Place ColorBySelector in template**: Add it to the `.dashboard-header .header-controls` div (lines 12-26), positioned BEFORE the theme toggle button. Bind:
   - `:model-value="colorByAttribute"`
   - `:options="colorByOptions"`
   - `@update:model-value="colorByAttribute = $event"`
   - Only render with `v-if="colorByOptions.length > 0"` (don't show if no attributes configured)

5. **Ensure colorByAttribute prop passes to ALL card types**: Currently `color-by-attribute` is already passed at line 115 in the component template. Verify it reaches scatter, histogram, pie cards — it should because the template already has `:color-by-attribute="colorByAttribute"` on the dynamic component. No change needed here if already present.

6. **Ensure colorByOptions prop passes to ALL card types**: Currently at line 118. Already passed. No change needed.

**Important:** Do NOT remove MapCard's existing color-by dropdown yet. That happens in Plan 03 when MapCard is migrated to read from the shared state. For now, both selectors may coexist temporarily.
  </action>
  <verify>
- `npm run build` succeeds without TypeScript errors
- ColorBySelector.vue file exists in components/controls/
- InteractiveDashboard.vue imports and renders ColorBySelector in the header
- `grep -n "ColorBySelector" src/plugins/interactive-dashboard/InteractiveDashboard.vue` shows import and template usage
- `grep -n "yaml.colorBy" src/plugins/interactive-dashboard/InteractiveDashboard.vue` shows YAML parsing
  </verify>
  <done>
- ColorBySelector component renders a dropdown with "None" option first, followed by configured attributes
- Dashboard header shows the color-by selector when colorBy attributes are configured in YAML
- colorByAttribute state initializes to first configured attribute (not "None")
- Changing the selector updates colorByAttribute which flows to all cards via props
- Build passes with no errors
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes
2. ColorBySelector.vue exists at `src/plugins/interactive-dashboard/components/controls/`
3. InteractiveDashboard.vue renders ColorBySelector in `.header-controls`
4. Top-level `colorBy` YAML section is parsed and populates colorByOptions
5. colorByAttribute prop flows to all card components via template bindings
</verification>

<success_criteria>
- Central color-by dropdown appears in dashboard header when YAML has colorBy section
- Dropdown shows "None" + configured attributes
- First attribute is selected by default
- Changing selection updates the shared state that flows to all cards
</success_criteria>

<output>
After completion, create `.planning/phases/04.3-scatter-plot-color-by-selector-and-cross-card-sync/04.3-01-SUMMARY.md`
</output>
