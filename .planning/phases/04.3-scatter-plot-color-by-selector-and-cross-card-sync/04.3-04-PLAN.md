---
phase: 04.3-scatter-plot-color-by-selector-and-cross-card-sync
plan: 04
type: execute
wave: 3
depends_on: ["04.3-02", "04.3-03"]
files_modified:
  - src/plugins/interactive-dashboard/InteractiveDashboard.vue
autonomous: false

must_haves:
  truths:
    - "All 4 card types show consistent colors for the same category across the dashboard"
    - "Changing the central color-by selector updates ALL cards simultaneously"
    - "Selecting 'None' returns all cards to their default single-color rendering"
    - "Legend click-to-filter filters data across all cards"
    - "Comparison mode works correctly with color-by active (baseline gray, filtered colored)"
    - "No Plotly event handler memory leaks (no MaxListenersExceededWarning in console)"
  artifacts:
    - path: "src/plugins/interactive-dashboard/InteractiveDashboard.vue"
      provides: "Complete dashboard with central color-by, legend, and cross-card sync"
  key_links:
    - from: "ColorBySelector"
      to: "All card components"
      via: "colorByAttribute prop flow through InteractiveDashboard template"
      pattern: "color-by-attribute"
    - from: "ColorLegend click"
      to: "FilterManager"
      via: "handleDashboardLegendClick → filterManager.setFilter()"
      pattern: "handleDashboardLegendClick"
---

<objective>
Verify complete cross-card color-by synchronization, fix integration issues, and validate all user requirements are met.

Purpose: Ensures the phase delivers a working, polished feature with no regressions in existing functionality.

Output: Verified and polished implementation ready for use.
</objective>

<execution_context>
@/home/christoph/.claude/get-shit-done/workflows/execute-plan.md
@/home/christoph/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04.3-scatter-plot-color-by-selector-and-cross-card-sync/04.3-CONTEXT.md
@.planning/phases/04.3-scatter-plot-color-by-selector-and-cross-card-sync/04.3-01-SUMMARY.md
@.planning/phases/04.3-scatter-plot-color-by-selector-and-cross-card-sync/04.3-02-SUMMARY.md
@.planning/phases/04.3-scatter-plot-color-by-selector-and-cross-card-sync/04.3-03-SUMMARY.md
@src/plugins/interactive-dashboard/InteractiveDashboard.vue
@src/plugins/interactive-dashboard/components/cards/ScatterCard.vue
@src/plugins/interactive-dashboard/components/cards/HistogramCard.vue
@src/plugins/interactive-dashboard/components/cards/PieChartCard.vue
@src/plugins/interactive-dashboard/components/cards/MapCard.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration fixes and consistency validation</name>
  <files>src/plugins/interactive-dashboard/InteractiveDashboard.vue</files>
  <action>
Run `npm run build` and fix any TypeScript compilation errors across all modified files.

Then verify the following integration points by reading and tracing the code path:

1. **Prop flow consistency**: Ensure `colorByAttribute` and `colorByOptions` are passed to ALL card components via the InteractiveDashboard template. The props should be on the dynamic `component` element (around line 115-118). Verify all card types have corresponding prop definitions.

2. **Type detection consistency**: Ensure the type detection logic (`detectColorByType`) uses the same threshold (15 unique values) across ScatterCard, HistogramCard, and PieChartCard. If the logic was duplicated, consider extracting to a shared utility function in `src/plugins/interactive-dashboard/utils/colorByUtils.ts` with:
   ```typescript
   export function detectColorByType(data: any[], attribute: string): 'categorical' | 'numeric'
   export function getColorByColorMap(data: any[], attribute: string): Map<string, string>
   export function getColorByNumericRange(data: any[], attribute: string): [number, number]
   ```
   This ensures all cards compute the same colors for the same data. If already consistent, skip extraction.

3. **YAML backward compatibility**: Verify that dashboards with `map.colorBy` (old location) still work when `colorBy` (new top-level location) is not present. The `colorByOptions` computed and `mounted()` initialization should fall back correctly.

4. **Legend filter interaction**: When a legend category is clicked and filtered, verify that:
   - The filter propagates through FilterManager to all cards
   - The color-by rendering in each card respects the filter (shows only matching data)
   - The filter can be cleared by clicking the same category again or pressing Escape

5. **Comparison mode with color-by**: When comparison mode is active AND color-by is active:
   - Baseline data in all charts should be gray (NOT colored by attribute)
   - Only filtered data gets color-by colors
   - Verify this for all 4 card types

Fix any issues found. Common issues to watch for:
- Plotly `react()` not being called (only `newPlot()`) causing handler loss
- Color mismatch between legend and chart (different sort order of categories)
- Missing `filterVersion` increment after legend filter updates
- MapCard's `hasMapControls` computed returning false when only geometryType selector should show
  </action>
  <verify>
- `npm run build` succeeds with zero TypeScript errors
- `npm run test:run` passes (if tests exist)
- No console warnings about missing props or undefined values
  </verify>
  <done>
- Build passes cleanly
- All integration points verified
- Type detection consistent across all cards
- YAML backward compatibility preserved
- Legend filtering works through FilterManager
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of cross-card color sync</name>
  <files>src/plugins/interactive-dashboard/InteractiveDashboard.vue</files>
  <action>
Human visual verification of the complete cross-card color-by synchronization feature.

What was built:
1. Central color-by dropdown in dashboard header
2. ScatterCard with categorical multi-trace and numeric colorscale
3. HistogramCard with stacked categorical bars and average-colored numeric bars
4. PieChartCard with categorical slice grouping by color-by attribute
5. MapCard reading from shared state (per-card dropdown removed)
6. Dashboard-level ColorLegend with click-to-filter

Steps to verify:
1. Start the dev server: `npm run dev`
2. Open a dashboard with a colorBy YAML configuration
3. Verify the color-by dropdown appears in the dashboard header area
4. Select a categorical attribute (e.g., "Transport Mode"):
   - ALL cards should update simultaneously
   - Scatter: points colored by category with legend
   - Histogram: stacked bars showing category distribution
   - Pie: slices grouped by the color-by attribute
   - Map: layers colored by attribute
   - Legend: shows category colors matching all charts
5. Select "None":
   - All cards return to default single-color rendering
   - Legend disappears
6. Select a numeric attribute (e.g., "Distance"):
   - Scatter: points colored with Viridis gradient
   - Histogram: bars colored by average value
   - Pie: unchanged (ignores numeric color-by)
   - Map: layers colored with gradient
   - Legend: shows numeric gradient bar
7. Click a legend category (categorical mode):
   - Data filters across all cards
   - Click again to unfilter
8. Enable comparison mode and verify:
   - Baseline stays gray in all charts
   - Filtered data shows color-by colors
9. Check browser console for any warnings (especially MaxListenersExceededWarning)
  </action>
  <verify>User confirms all visual checks pass</verify>
  <done>All cross-card color-by features verified working by user</done>
</task>

</tasks>

<verification>
1. `npm run build` passes
2. Central color-by selector controls ALL card types
3. Color consistency verified across scatter, histogram, pie, and map
4. "None" option disables color-by cleanly
5. Legend click-to-filter works
6. Comparison mode interaction correct
7. No Plotly memory leaks
8. YAML backward compatibility maintained
</verification>

<success_criteria>
- User can select a color-by attribute and see ALL cards update simultaneously
- User can select "None" and see all cards return to default colors
- User can click legend entries to filter data across all cards
- Comparison mode preserves gray baseline with colored filtered data
- No console errors or memory leak warnings
</success_criteria>

<output>
After completion, create `.planning/phases/04.3-scatter-plot-color-by-selector-and-cross-card-sync/04.3-04-SUMMARY.md`
</output>
