---
phase: 04.2-scientific-mode
plan: 07
type: execute
wave: 4
depends_on: ["04.2-03", "04.2-05", "04.2-06"]
files_modified:
  - src/plugins/interactive-dashboard/InteractiveDashboard.vue
  - src/plugins/interactive-dashboard/components/DashboardCard.vue
autonomous: false

must_haves:
  truths:
    - "Scientific mode can be toggled from dashboard UI"
    - "All charts display with white background and black axes when scientific mode active"
    - "Export All downloads ZIP with all charts in scientific styling"
    - "Per-card export buttons work for all card types"
  artifacts: []
  key_links: []
---

<objective>
Verify and polish the complete scientific mode implementation.

Purpose: Ensure all Phase 4.2 features work end-to-end: mode switching, styling, and export functionality.

Output: Verified scientific mode with all requirements (SCI-01 to SCI-05) met.
</objective>

<execution_context>
@/home/christoph/.claude/get-shit-done/workflows/execute-plan.md
@/home/christoph/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.2-scientific-mode/04.2-03-SUMMARY.md
@.planning/phases/04.2-scientific-mode/04.2-05-SUMMARY.md
@.planning/phases/04.2-scientific-mode/04.2-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run dev server and test scientific mode styling</name>
  <files>N/A</files>
  <action>
Start the development server and test scientific mode:

```bash
cd /mnt/Shared/Code/projects/Dissertation/simwrapper
npm run dev
```

Then navigate to a dashboard with multiple chart types and:

1. Toggle to scientific mode (click theme toggle until scientific mode activates)
2. Verify visual styling for each card type:
   - HistogramCard: white background, black axes, Arial font
   - ScatterCard: white background, black axes, Arial font
   - PieChartCard: white background, Arial font for legend
   - CorrelationMatrixCard: white background, black labels
   - TimelineCard: white background, zoom controls hidden
   - MapCard: navigation controls hidden

3. Note any styling issues that need fixing.
  </action>
  <verify>Visual inspection confirms scientific styling applied to all chart types</verify>
  <done>Scientific mode styling verified for all card types</done>
</task>

<task type="auto">
  <name>Task 2: Test per-card export functionality</name>
  <files>N/A</files>
  <action>
Test export button on each card type:

1. For each card type (histogram, scatter, pie, correlation, timeline, map):
   - Click the export button (download icon)
   - Select PNG format
   - Verify file downloads with correct filename
   - Select SVG format (for Plotly charts)
   - Verify SVG downloads correctly

2. Verify export respects card's exportName if configured in YAML

3. Test that non-exportable cards (e.g., text/markdown) don't show export button

4. Note any export issues that need fixing.
  </action>
  <verify>All card types export correctly as PNG, Plotly charts also export as SVG</verify>
  <done>Per-card export functionality verified</done>
</task>

<task type="auto">
  <name>Task 3: Test Export All functionality</name>
  <files>N/A</files>
  <action>
Test the Export All button:

1. Click "Export All" button in dashboard header
2. Verify spinner shows during export
3. Verify ZIP file downloads
4. Extract ZIP and check contents:
   - All charts present as individual PNG files
   - Filenames match chart titles (sanitized)
   - Images have correct dimensions and resolution

5. Test with scientific mode enabled:
   - Exported images should have scientific styling

6. Test edge cases:
   - Dashboard with no charts (graceful handling)
   - Dashboard with only map cards
  </action>
  <verify>Export All generates ZIP with all charts, respects scientific styling</verify>
  <done>Export All functionality verified</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete scientific mode implementation:
- Three-way theme switching (Dark/Light/Scientific)
- Scientific styling for all chart types (white bg, black axes, Arial font)
- Per-card export buttons with PNG/SVG options
- Export All button generating ZIP archive
- Map export via canvas snapshot
  </what-built>
  <how-to-verify>
1. Open http://localhost:8080 in browser
2. Navigate to an interactive dashboard with multiple card types
3. Click theme toggle to cycle through Dark -> Light -> Scientific
4. In Scientific mode, verify:
   - All charts have white background
   - Axes are black with thicker lines
   - Font is Arial/Helvetica
   - Plotly modebar is hidden
   - Map zoom controls are hidden
5. Test per-card export:
   - Click download icon on a histogram card
   - Select PNG - verify download
   - Select SVG - verify download
6. Test Export All:
   - Click "Export All" in dashboard header
   - Extract downloaded ZIP
   - Verify all charts present with correct styling

Expected: All visual and functional requirements met (SCI-01 to SCI-05)
  </how-to-verify>
  <resume-signal>Type "approved" if all requirements met, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Development server runs without errors
2. Scientific mode styling applied to all chart types
3. Per-card export works for all exportable card types
4. Export All generates complete ZIP archive
5. All requirements SCI-01 through SCI-05 satisfied
</verification>

<success_criteria>
Requirements verification:
- SCI-01: Scientific styling profile (white bg, black text/axes, Arial font) - VERIFIED
- SCI-02: Clean plot layouts (no modebar, no zoom controls) - VERIFIED
- SCI-03: Per-plot export button (PNG/SVG) - VERIFIED
- SCI-04: Bulk export "Download All" with ZIP - VERIFIED
- SCI-05: Export configuration via YAML (exportConfig, exportName) - VERIFIED
</success_criteria>

<output>
After completion, create `.planning/phases/04.2-scientific-mode/04.2-07-SUMMARY.md`
</output>
